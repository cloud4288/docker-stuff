version: '2'

services:
  mysql:
    image: mysql
    ports:
      - "3306:3306"
    volumes:
        - mysql_data:/var/lib/mysql
    environment:
        MYSQL_ROOT_PASSWORD: 'password'

volumes:
  mysql_data: